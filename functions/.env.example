# Optional non-secret env vars for local development.
# Do NOT set GEMINI_API_KEY here — it conflicts with the deployed secret of the same name.
#
# For local emulator / Genkit:
#   copy .secret.local.example → .secret.local
# For production:
#   firebase functions:secrets:set GEMINI_API_KEY
